import classes from './Button.module.css';

export const Button = () => {
  return (
    <div>
      <a href="/" className={classes.projectButton}>
        RELEASE CALENDAR
      </a>
    </div>
  );
};
